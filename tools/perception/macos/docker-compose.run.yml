# Started only by start.sh. Keep the camera stack in its own Compose project.
services:
  aha_project:
    command: ["bash", "/app/tools/perception/macos/container_start.sh"]
    ipc: private

  novnc:
    ports: !override
      - "127.0.0.1:8080:8080"
