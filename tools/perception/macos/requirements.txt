# Mac host dependencies for the RealSense -> rosbridge development path.
# Python 3.11 is recommended because pyrealsense2 wheel availability depends
# on the Python and CPU architecture combination.
numpy>=1.26,<3
websocket-client>=1.8,<2
# PyPI does not publish a matching wheel for macOS ARM64.  On macOS the SDK
# binding must be built from the librealsense source; see the README.
pyrealsense2>=2.55.1; platform_system != "Darwin"
